# AI Memory System v1.5.0 技术方案设计

**版本**: v1.5.0  
**代号**: "性能革命"  
**目标**: 解决核心性能瓶颈，提升 10 倍写入速度  
**计划发布日期**: 2026-04-30  

---

## 🎯 总体目标

### 核心问题解决
1. **P0-1**: Markdown 全量写入 O(n) → WAL 增量写入 O(1)
2. **P0-2**: 权重系统独立 → 集成到检索核心
3. **P0-3**: Beta 功能误导 → 明确标记 + 功能开关

### 性能指标
| 场景 | v1.4.0 | v1.5.0 目标 | 验证方法 |
|------|--------|------------|----------|
| 写入 1000 条记忆 | 60s+ | < 10s | `benchmark_write.py` |
| 并发 32 线程 | 30s 超时 | 无超时 | `benchmark_concurrent.py` |
| 检索 1000 条 | 2-5s | < 1s | `benchmark_search.py` |
| 内存峰值 | 500MB+ | < 100MB | 内存监控 |

---

## 📁 架构变更

### 当前架构
```
memory-bank/
├── long-term-memory-*.md    # 全量分片文件
├── weights.json             # 独立权重文件
├── knowledge-base-*.md      # KB 分片
└── sessions/               # 会话文件
```

### v1.5.0 架构
```
memory-bank/
├── ltm/                    # LTM 存储目录
│   ├── profile.md         # 分片文件 (只读基础)
│   ├── preference.md
│   └── ...
├── ltm_wal/               # WAL 目录
│   ├── profile.jsonl      # 增量写入文件
│   ├── preference.jsonl
│   └── ...
├── kb/                    # KB 存储目录
│   └── ... (类似结构)
├── weights/               # 权重目录
│   ├── ltm_weights.json   # LTM 权重
│   └── kb_weights.json    # KB 权重
└── index/                 # 索引目录
    ├── ltm_inverted.json  # 倒排索引
    └── kb_inverted.json   # KB 倒排索引
```

---

## 🔧 P0-1: WAL 机制实现方案

### 1. WAL 文件格式
```json
// ltm_wal/profile.jsonl
{"id": "ltm-001", "content": "我喜欢Python", "category": "preference", "op": "add", "ts": "2026-04-08T20:58:00Z"}
{"id": "ltm-002", "content": "我不喜欢Java", "category": "preference", "op": "add", "ts": "2026-04-08T20:58:01Z"}
{"id": "ltm-001", "op": "delete", "ts": "2026-04-08T20:59:00Z"}  // 删除操作
```

### 2. 核心类设计
```python
# engine/core/wal_manager.py
class WALManager:
    """Write-Ahead Log 管理器"""
    
    def __init__(self, wal_dir: Path):
        self.wal_dir = wal_dir
        self._category_files: dict[str, TextIO] = {}
        self._lock = threading.RLock()
    
    def append(self, category: str, entry: dict) -> str:
        """追加一条记录到 WAL"""
        with self._lock:
            file_path = self.wal_dir / f"{category}.jsonl"
            # 如果文件不存在，创建并写入头部
            if not file_path.exists():
                file_path.write_text("# WAL for " + category + "\n")
            
            # 追加 JSONL 记录
            with open(file_path, 'a', encoding='utf-8') as f:
                json_line = json.dumps(entry, ensure_ascii=False)
                f.write(json_line + "\n")
            
            return file_path.name
    
    def compact(self, category: str, target_shard: Path) -> bool:
        """合并 WAL 到目标分片文件"""
        # 1. 读取现有分片
        # 2. 应用 WAL 中的所有操作
        # 3. 写回新分片
        # 4. 清空 WAL 文件
        pass
```

### 3. LTMManager 改造
```python
# engine/core/ltm.py (改造部分)
class LTMManager:
    def __init__(self, memory_dir: Path):
        self.memory_dir = memory_dir
        self.wal_manager = WALManager(memory_dir / "ltm_wal")
        self._entries_cache: dict[str, list[LTMEntry]] = {}  # 内存缓存
    
    def save(self, content: str, category: str = "general", **kwargs) -> LTMEntry:
        """保存新记忆（WAL 优先）"""
        # 1. 生成条目
        entry = LTMEntry(
            id=f"ltm-{uuid4().hex[:8]}",
            content=content,
            category=category,
            # ... 其他字段
        )
        
        # 2. 写入 WAL（快速）
        wal_record = {
            "id": entry.id,
            "content": content,
            "category": category,
            "op": "add",
            "ts": _now_iso(),
            "weight": kwargs.get("weight", 2),  # 默认权重
            "tags": kwargs.get("tags", []),
        }
        self.wal_manager.append(category, wal_record)
        
        # 3. 更新内存缓存
        if category not in self._entries_cache:
            self._entries_cache[category] = []
        self._entries_cache[category].append(entry)
        
        return entry
    
    def _load_shard(self, category: str) -> list[LTMEntry]:
        """加载分片（合并 WAL）"""
        # 1. 读取基础分片文件
        shard_path = self.memory_dir / f"long-term-memory-{category}.md"
        base_entries = self._load_shard_file(shard_path) if shard_path.exists() else []
        
        # 2. 读取并应用 WAL
        wal_entries = self._load_wal_for_category(category)
        
        # 3. 合并：base_entries + wal 操作
        return self._merge_entries(base_entries, wal_entries)
```

### 4. 合并策略
```python
def _merge_entries(base: list[LTMEntry], wal_ops: list[dict]) -> list[LTMEntry]:
    """合并基础条目和 WAL 操作"""
    merged = {e.id: e for e in base}
    
    for op in wal_ops:
        if op["op"] == "add":
            merged[op["id"]] = LTMEntry(
                id=op["id"],
                content=op["content"],
                category=op["category"],
                created_at=op.get("created_at", op["ts"]),
                updated_at=op["ts"],
                weight=op.get("weight", 2),
                tags=op.get("tags", []),
            )
        elif op["op"] == "update":
            if op["id"] in merged:
                merged[op["id"]].content = op.get("content", merged[op["id"]].content)
                merged[op["id"]].updated_at = op["ts"]
                merged[op["id"]].weight = op.get("weight", merged[op["id"]].weight)
        elif op["op"] == "delete":
            merged.pop(op["id"], None)
    
    return list(merged.values())
```

### 5. 定期合并任务
```python
# engine/core/compactor.py
class Compactor:
    """WAL 合并器"""
    
    def __init__(self, ltm_manager: LTMManager, interval_minutes: int = 60):
        self.ltm = ltm_manager
        self.interval = interval_minutes
        self._running = False
        self._thread: Optional[threading.Thread] = None
    
    def start(self):
        """启动后台合并线程"""
        self._running = True
        self._thread = threading.Thread(target=self._run, daemon=True)
        self._thread.start()
    
    def _run(self):
        """后台运行合并"""
        while self._running:
            time.sleep(self.interval * 60)
            
            # 合并所有有 WAL 的分类
            for category in self.ltm.wal_manager.get_categories():
                try:
                    self.ltm.compact_category(category)
                except Exception as e:
                    logger.error(f"合并分类 {category} 失败: {e}")
```

---

## 🔧 P0-2: 权重系统集成方案

### 1. LTMEntry 扩展
```python
# engine/core/models.py
@dataclass
class LTMEntry:
    id: str
    content: str
    category: str
    source: str = "user-explicit"
    tags: list[str] = field(default_factory=list)
    created_at: str = field(default_factory=_now_iso)
    updated_at: str = field(default_factory=_now_iso)
    sensitive: bool = False
    encrypted_ref: Optional[str] = None
    similar_to: Optional[str] = None
    
    # 🔥 新增字段
    weight: int = 2  # 默认 MEDIUM 权重
    access_count: int = 0  # 访问次数
    last_accessed: Optional[str] = None  # 最后访问时间
    importance_score: float = 0.0  # 重要性评分 (0-1)
```

### 2. 权重管理类
```python
# engine/core/weight_manager.py
class WeightManager:
    """权重管理器（替换原来的 weight.py）"""
    
    # 权重级别
    LOW = 1
    MEDIUM = 2
    HIGH = 3
    CRITICAL = 4
    CORE = 5
    
    def __init__(self, memory_dir: Path):
        self.memory_dir = memory_dir
        self._weights_file = memory_dir / "weights" / "ltm_weights.json"
        self._weights: dict[str, int] = self._load_weights()
    
    def _load_weights(self) -> dict[str, int]:
        """加载权重文件"""
        if self._weights_file.exists():
            with open(self._weights_file, 'r', encoding='utf-8') as f:
                return json.load(f)
        return {}
    
    def get_weight(self, entry_id: str) -> int:
        """获取条目权重"""
        return self._weights.get(entry_id, self.MEDIUM)
    
    def set_weight(self, entry_id: str, weight: int) -> None:
        """设置权重"""
        self._weights[entry_id] = weight
        self._save_weights()
    
    def auto_suggest_weight(self, content: str, category: str) -> int:
        """自动建议权重（改进版）"""
        # 1. 关键词匹配
        critical_keywords = ["密码", "token", "密钥", "credential", "secret"]
        if any(kw in content for kw in critical_keywords) or category == "credential":
            return self.CRITICAL
        
        # 2. 分类重要性
        category_weights = {
            "profile": self.HIGH,      # 用户档案
            "preference": self.HIGH,   # 偏好
            "decision": self.HIGH,     # 决策
            "project": self.MEDIUM,    # 项目
            "technical": self.MEDIUM,  # 技术
            "general": self.LOW,       # 一般
        }
        
        return category_weights.get(category, self.MEDIUM)
    
    def calculate_importance_score(self, entry: LTMEntry) -> float:
        """计算重要性评分 (0-1)"""
        # 基础权重 (0.2-1.0)
        weight_score = entry.weight / 5.0  # 1→0.2, 5→1.0
        
        # 访问频率 (0-0.3)
        if entry.access_count > 0:
            freq_score = min(0.3, entry.access_count * 0.05)
        else:
            freq_score = 0
        
        # 时间衰减 (0-0.5)
        if entry.last_accessed:
            days_diff = (_parse_iso(entry.last_accessed) - datetime.now()).days
            if days_diff <= 7:
                time_score = 0.5
            elif days_diff <= 30:
                time_score = 0.3
            else:
                time_score = 0.1
        else:
            time_score = 0.2
        
        return weight_score + freq_score + time_score
```

### 3. 检索时自动应用权重
```python
# engine/core/ltm.py (search 方法改造)
def search(self, query: str, category: str | None = None, 
           max_results: int = 20, use_weight: bool = True) -> list[LTMEntry]:
    """搜索记忆（自动权重排序）"""
    # 1. 获取所有条目
    all_entries = self._load_shard(category) if category else self._load_entries()
    
    # 2. 基础搜索（BM25 或关键词）
    if _BM25_AVAILABLE and len(all_entries) >= 1:
        corpus = [_tokenize(e.content) for e in all_entries]
        bm25 = _BM25Okapi(corpus)
        query_tokens = _tokenize(query)
        scores = bm25.get_scores(query_tokens)
        
        # 创建 (score, entry) 对
        scored_entries = [(scores[i], all_entries[i]) for i in range(len(all_entries))]
    else:
        # 关键词匹配降级
        query_lower = query.lower()
        scored_entries = [
            (1.0 if query_lower in e.content.lower() else 0.0, e)
            for e in all_entries
        ]
    
    # 3. 🔥 应用权重调整
    if use_weight:
        weighted_entries = []
        for score, entry in scored_entries:
            # 计算重要性评分
            importance = self.weight_manager.calculate_importance_score(entry)
            
            # 最终评分 = 搜索相关性 × 重要性
            final_score = score * importance
            
            weighted_entries.append((final_score, entry))
        
        # 按最终评分排序
        weighted_entries.sort(key=lambda x: x[0], reverse=True)
        result_entries = [e for _, e in weighted_entries[:max_results]]
    else:
        # 不使用权重
        scored_entries.sort(key=lambda x: x[0], reverse=True)
        result_entries = [e for _, e in scored_entries[:max_results]]
    
    # 4. 更新访问记录
    for entry in result_entries:
        entry.access_count += 1
        entry.last_accessed = _now_iso()
    
    return result_entries
```

### 4. 权重迁移工具
```python
# scripts/migrate_weights.py
def migrate_weights_to_entries():
    """将 weights.json 迁移到 LTMEntry 中"""
    # 1. 读取旧权重
    old_weights_path = Path("memory-bank/weights.json")
    if not old_weights_path.exists():
        print("⚠️ 无旧权重文件可迁移")
        return
    
    with open(old_weights_path, 'r', encoding='utf-8') as f:
        old_weights = json.load(f)
    
    # 2. 遍历所有 LTM 条目
    ltm = LTMManager(Path("memory-bank"))
    migrated_count = 0
    
    for category in ["profile", "preference", "decision", "project", "technical", "general"]:
        entries = ltm._load_shard(category)
        for entry in entries:
            if entry.id in old_weights:
                entry.weight = old_weights[entry.id]
                migrated_count += 1
    
    # 3. 保存更新
    ltm._save_all_shards()
    
    # 4. 备份并删除旧文件
    old_weights_path.rename(f"weights_backup_{_now_iso()}.json")
    
    print(f"✅ 迁移完成: {migrated_count} 条权重记录")
```

---

## 🔧 P0-3: Beta 功能明确标记方案

### 1. 入口警告
```python
# adaptive_skill_system.py (开头添加)
"""
⚠️⚠️⚠️ 自适应技能系统 (Beta/实验性) ⚠️⚠️⚠️

重要说明：
1. 当前版本是占位符实现，无法真正执行技能
2. 质量评估依赖简化启发式规则
3. 技能进化逻辑未经充分验证

使用前请设置环境变量：
export USE_BETA_SKILL_SYSTEM=true

或在代码中显式启用：
os.environ["USE_BETA_SKILL_SYSTEM"] = "true"
"""

import os
import warnings

# 检查是否启用
if os.getenv("USE_BETA_SKILL_SYSTEM", "false").lower() != "true":
    warnings.warn(
        "自适应技能系统为 Beta 功能，默认禁用。"
        "如需使用，请设置 USE_BETA_SKILL_SYSTEM=true",
        UserWarning
    )
    # 返回简化版或抛出异常
    class AdaptiveSkillSystem:
        def __init__(self):
            raise RuntimeError(
                "自适应技能系统需要显式启用。"
                "请设置环境变量 USE_BETA_SKILL_SYSTEM=true"
            )
else:
    # 正常初始化
    warnings.warn("正在使用 Beta 版自适应技能系统，功能可能不完整", UserWarning)
```

### 2. 执行器明确标记
```python
def _execute_template_step(self, skill_draft: "SkillDraft", **kwargs) -> str:
    """执行模板生成步骤（占位符）"""
    return f"""
[BETA 占位符输出]
当前为自适应技能系统 Beta 版，无法真正生成技能。
技能名称: {skill_draft.name}
技能描述: {skill_draft.description}

在完整版中，这里会：
1. 调用 LLM 生成技能模板
2. 填充具体步骤和参数
3. 验证技能可行性

如需完整功能，请等待 v2.0 版本。
"""
```

### 3. 质量评估明确说明
```python
def evaluate_skill_quality(self, skill_content: str) -> "QualityEvaluation":
    """评估技能质量（简化启发式）"""
    return QualityEvaluation(
        overall_score=0.65,  # 固定分数，实际应计算
        dimensions={
            "completeness": 0.7,
            "clarity": 0.6,
            "feasibility": 0.5,
            "evidence": 0.7,
            "generalization": 0.8,
            "novelty": 0.4,
            "risk_mitigation": 0.6,
        },
        passed_threshold=False,
        notes="⚠️ 此为 Beta 版简化评估，实际质量需人工验证"
    )
```

### 4. 文档更新
在 README.md 中添加明确章节：
```markdown
## ⚠️ 实验性功能

### 自适应技能系统
当前版本为 **Beta/实验性** 功能，有以下限制：

1. **占位符实现**: 执行器无法真正生成技能，仅为文本拼接
2. **简化评估**: 质量评估依赖启发式规则，非真实 LLM 评估
3. **未经充分测试**: 进化逻辑和组合策略未经验证

**启用方法**:
```bash
export USE_BETA_SKILL_SYSTEM=true
```

**生产环境建议**: 不建议在生产环境使用此功能，等待 v2.0 完整实现。
```

---

## 📋 实施计划

### 阶段 1: WAL 机制 (2-3天)
| 任务 | 负责人 | 预计完成 | 状态 |
|------|--------|----------|------|
| 1.1 设计 WAL 格式 | 后石 | D1 | 🔄 |
| 1.2 实现 WALManager | 后石 | D2 | 🔄 |
| 1.3 改造 LTMManager.save() | 后石 | D2 | 🔄 |
| 1.4 实现合并逻辑 | 后石 | D3 | 🔄 |
| 1.5 编写测试用例 | 后石 | D3 | 🔄 |

### 阶段 2: 权重集成 (1-2天)
| 任务 | 负责人 | 预计完成 | 状态 |
|------|--------|----------|------|
| 2.1 扩展 LTMEntry | 后石 | D4 | 🔄 |
| 2.2 实现 WeightManager | 后石 | D4 | 🔄 |
| 2.3 改造 search() 方法 | 后石 | D5 | 🔄 |
| 2.4 编写迁移工具 | 后石 | D5 | 🔄 |
| 2.5 测试权重排序 | 后石 | D5 | 🔄 |

### 阶段 3: Beta 标记 (0.5天)
| 任务 | 负责人 | 预计完成 | 状态 |
|------|--------|----------|------|
| 3.1 添加入口警告 | 后石 | D6 | 🔄 |
| 3.2 更新执行器输出 | 后石 | D6 | 🔄 |
| 3.3 更新文档 | 后石 | D6 | 🔄 |

### 阶段 4: 测试与发布 (1-2天)
| 任务 | 负责人 | 预计完成 | 状态 |
|------|--------|----------|------|
| 4.1 性能基准测试 | 后石 | D7 | 🔄 |
| 4.2 集成测试 | 后石 | D7 | 🔄 |
| 4.3 编写升级指南 | 后石 | D8 | 🔄 |
| 4.4 发布 v1.5.0 | 后石 | D8 | 🔄 |

---

## 🧪 测试策略

### 1. 性能测试
```python
# tests/benchmark_write_performance.py
def test_wal_write_performance():
    """测试 WAL 写入性能"""
    # 写入 1000 条记录
    start = time.time()
    for i in range(1000):
        ltm.save(f"测试记忆 {i}", "test")
    elapsed = time.time() - start
    
    # 断言: < 10秒
    assert elapsed < 10.0, f"写入太慢: {elapsed:.2f}s"
```

### 2. 正确性测试
```python
# tests/test_wal_integrity.py
def test_wal_recovery():
    """测试 WAL 崩溃恢复"""
    # 1. 写入一些记录
    for i in range(10):
        ltm.save(f"记录 {i}", "test")
    
    # 2. 模拟崩溃（不调用 compact）
    # 3. 重新初始化
    new_ltm = LTMManager(memory_dir)
    
    # 4. 验证数据完整性
    entries = new_ltm.search("记录")
    assert len(entries) == 10
```

### 3. 权重测试
```python
# tests/test_weight_integration.py
def test_weight_affects_search():
    """测试权重影响搜索结果"""
    # 创建高低权重条目
    ltm.save("重要记忆", "test", weight=5)  # CORE
    ltm.save("普通记忆", "test", weight=2)  # MEDIUM
    
    # 搜索
    results = ltm.search("记忆")
    
    # 重要记忆应该在前
    assert results[0].content == "重要记忆"
    assert results[0].weight == 5
```

---

## 📊 成功标准

### 技术标准
1. ✅ WAL 机制: 写入速度提升 6 倍 (< 10s/1000条)
2. ✅ 权重集成: 检索结果按权重排序
3. ✅ Beta 标记: 用户明确知道实验性状态
4. ✅ 向后兼容: v1.4.0 数据可无缝迁移

### 质量标准
1. ✅ 测试覆盖率 > 80%
2. ✅ 无回归测试失败
3. ✅ 文档完整，包含升级指南
4. ✅ 性能监控基线建立

### 用户体验
1. ✅ 写入响应明显加快
2. ✅ 检索结果更相关
3. ✅ 实验功能明确标注
4. ✅ 升级过程简单无痛

---

## 🔄 回滚计划

如果 v1.5.0 出现问题，可按以下步骤回滚：

### 1. 数据回滚
```bash
# 恢复旧权重文件
cp weights_backup_*.json memory-bank/weights.json

# 删除 WAL 文件
rm -rf memory-bank/ltm_wal/
rm -rf memory-bank/kb_wal/
```

### 2. 代码回滚
```bash
# 切换回 v1.4.0 标签
git checkout v1.4.0

# 重新安装
pip install -e .
```

### 3. 验证回滚
```bash
# 运行完整性检查
python scripts/check_integrity.py

# 运行基础测试
pytest tests/test_basic_functionality.py
```

---

## 📝 下一步行动

### 立即行动
1. [x] 审阅本技术方案
2. [ ] 确认可行性 (如有疑问请提出)
3. [ ] 分配具体实施任务

### 开发前准备
1. [ ] 创建特性分支: `feature/v1.5.0-performance`
2. [ ] 设置开发环境
3. [ ] 编写详细设计文档

### 开发中监控
1. [ ] 每日进度更新
2. [ ] 持续集成测试
3. [ ] 性能基准对比

---

**文档版本**: v1.0  
**创建时间**: 2026-04-08  
**创建者**: 后石 (后端工程师)  
**状态**: 🔄 待审阅